spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mangban_dev?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always
      continue-on-error: true
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql

logging:
  level:
    com.mangban: debug